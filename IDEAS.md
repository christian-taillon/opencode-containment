# Ideas

- Replace `scripts/nvim-wrapper` with a cleaner runtimepath solution (for example, setting the parser path directly via nvim startup config or environment) so we can remove the wrapper shim and keep the image behavior more transparent.
